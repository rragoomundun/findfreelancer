version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build --configuration development
  artifacts:
    baseDirectory: dist/findfreelancer
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
